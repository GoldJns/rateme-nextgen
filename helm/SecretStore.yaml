apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: dev-secret-store
  namespace: dev
spec:
  provider:
    gcpsm:
      projectID: rateme-nextgen #GCP projectID
      auth:
        workloadIdentity:
          clusterLocation: us-central1-c
          clusterName: rateme-nextgen-cluster
          serviceAccountRef:
            name: dev-service-account #KSA_NAME
